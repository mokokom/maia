<div class="form-container">
  <div class="form-centered">
    <h2>Ajouter un enfant à votre tableau de bord</h2>


<% if @kid.photo.attached? %>
  <%= cl_image_tag @kid.photo.key, crop: :file, gravity: :face %>
<% end %>


    <h2>Editer le profil</h2>
    <%= simple_form_for(@kid) do |f| %>
    <%= f.input :photo, as: :file %>
    <%= f.input :first_name %>
    <%= f.input :age %>
    <%= f.input :weight %>
    <%= f.button :submit  %>
    <% end %>
  </div>
</div>
